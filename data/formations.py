"""import numpy as np
from core.formation_generator import circle_formation

formation = circle_formation(num_points=5, radius=2.0)
np.save("data/formation.npy", formation)

# Caricamento
formation_loaded = np.load("data/formation.npy", allow_pickle=True).item()"""
